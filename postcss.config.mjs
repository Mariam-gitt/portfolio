// Export the PostCSS plugins required by Tailwind CSS v4.
export default {
  // Register Tailwind's PostCSS plugin.
  plugins: {
    // Let Tailwind transform utility classes into CSS.
    "@tailwindcss/postcss": {},
  },
};
