// Import the Next.js configuration type so this file is type-safe.
import type { NextConfig } from "next";

// Store the application's Next.js configuration in one object.
const nextConfig: NextConfig = {
  // Keep the default Next.js configuration because this portfolio needs no custom server behavior.
};

// Export the configuration so Next.js can read it when building or running the project.
export default nextConfig;
